<div class="row">
    <div class="col-md-6 eatem">
        <h2>Eat'em Up!</h2>
        <ul>
            {{#each burgers}} {{#unless devoured}} {{> partials/burger-block eat=true}} {{/unless}} {{/each}}
        </ul>
    </div>
    <div class="col-md-6 yum">
        <h2>Yum, Yum!</h2>
        <ul>
            {{#each burgers}} {{#if devoured}} {{> partials/burger-block eat=false}} {{/if}} {{/each}}
        </ul>
    </div>
</div>